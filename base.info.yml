name: Base
type: theme
description: Base inherits from Stable
core: 8.x
# Defines the base theme
base theme: stable
# Defines libraries group in which we can add css/js.
libraries:
  - base/global-styling
  - base/test-module
# Regions
regions:
  header: Header
  featured: Featured
  content: Content
  sidebar_first: First sidebar
  sidebar_second: Second sidebar
  footer: Footer